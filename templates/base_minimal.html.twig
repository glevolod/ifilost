<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.80.0">
    <title>{% block title %}If I'm lost{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('minimal') }}
    {% endblock %}
    <style>
        html {
            position: relative;
            min-height: 100%;
        }

        body {
            margin-bottom: 60px; /* Margin bottom by footer height */
        }

        .footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 60px; /* Set the fixed height of the footer here */
            line-height: 60px; /* Vertically center the text there */
            background-color: lightgray;
        }
    </style>
</head>
<body>

<header>
    <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 col-md-7 py-4">
                    <h4 class="text-white">{{ 'О сервисе'|trans }}</h4>
                    <p class="text-muted">{{ 'Сервис позволяет добавить контакты друзей (email адреса), на которые придут оповещения, если вы не подтвердите отметку в нем в указанное вами время'|trans }}</p>
                </div>
                <div class="col-sm-4 offset-md-1 py-4">
                    <h4 class="text-white">{{ 'Связаться'|trans }}</h4>
                    <ul class="list-unstyled">
                        <li><a href="mailto:{{ appMailAdmin }}" class="text-white">{{ appMailAdmin }}</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container d-flex justify-content-between">
            <a href="/" class="navbar-brand d-flex align-items-center">
                <img src="{{ asset('build/images/logo1_white.png') }}" height="30" class="mr-2">
                <strong>If you lost me</strong>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader"
                    aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
                <i class="fas fa-bars"></i>
            </button>
        </div>
    </div>
</header>

<main role="main">
    <!-- Main content -->
    <section class="content pt-5">
        <div class="container">
            {% for label, messages in app.flashes %}
                <div class="row">
                    <div class="offset-md-3 col-md-6">
                        {% for message in messages %}
                            <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                                {{ message|raw }}
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endfor %}
            {% block body %}
            {% endblock %}
            <!-- /.row -->
        </div>
        <!-- /.container-fluid -->
    </section>
</main>
<footer class="footer">
    <div class="container">
        <p class="float-left">
            &copy; "IfYouLostMe" ifyoulostme.com
        </p>
        <p class="float-right">
            Mailto: <a href="mailto:{{ appMailAdmin }}" class="text-dark">{{ appMailAdmin }}</a>
        </p>
    </div>
</footer>
{% block javascripts %}
    {{ encore_entry_script_tags('minimal') }}
{% endblock %}
</body>
</html>
